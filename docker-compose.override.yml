services:
  app:
    volumes:
      - ./:/app
    command: ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    environment:
      - LOG_LEVEL=DEBUG
      - IP_WHITELIST=
    ports:
      - "${MCP_HOST_PORT:-8000}:8000"